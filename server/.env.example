PORT=5000
# Optional for local dev: leave unset, set to "memory", or use Atlas/local URI.
# If your URI is unreachable, the server falls back to in-memory Mongo (non-production only).
# MONGO_URI=mongodb://127.0.0.1:27017/comment-threads
JWT_SECRET=change_this_to_a_long_random_secret
CLIENT_URL=http://localhost:5173
